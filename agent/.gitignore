# Audio, never committed. This repo is public, and vedanth_sample.mp3 is the
# exact input needed to clone the voice at any provider. It came from a work
# meeting, so it is not ours alone to publish. Unanchored on purpose:
# extract-sample.py writes relative to the cwd.
*.mp3
*.wav
*.m4a

# Generated by voice/audition.py: regenerable, and each run spends characters.
voice/audition/

.DS_Store
